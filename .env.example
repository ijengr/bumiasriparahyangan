# ============================================
# APPLICATION CONFIGURATION
# ============================================
# Application name displayed in UI
APP_NAME="Bumi Asri Parahyangan"

# Environment: local, staging, production
# IMPORTANT: Set to 'production' for live deployment
APP_ENV=local

# Application key for encryption
# CRITICAL: Generate using 'php artisan key:generate'
# NEVER commit actual key to version control
APP_KEY=

# Debug mode - MUST be false in production
# Shows detailed error messages when true
APP_DEBUG=true

# Full URL of your application (including protocol)
# Example production: https://www.bumiasiparahyangan.com
APP_URL=http://localhost

# ============================================
# LOCALIZATION
# ============================================
APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

# ============================================
# MAINTENANCE & PERFORMANCE
# ============================================
APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

# Number of PHP workers for CLI server
# PHP_CLI_SERVER_WORKERS=4

# Password hashing cost (higher = more secure but slower)
# Recommended: 12 for production
BCRYPT_ROUNDS=12

# ============================================
# LOGGING CONFIGURATION
# ============================================
# Available channels: single, daily, slack, syslog, errorlog, stack
LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
# Levels: emergency, alert, critical, error, warning, notice, info, debug
LOG_LEVEL=debug

# ============================================
# DATABASE CONFIGURATION
# ============================================
# Using SQLite for simplicity (change to mysql/pgsql for production at scale)
DB_CONNECTION=sqlite
# For MySQL/MariaDB production setup:
# DB_CONNECTION=mysql
# DB_HOST=127.0.0.1
# DB_PORT=3306
# DB_DATABASE=bumi_asri_db
# DB_USERNAME=your_db_user
# DB_PASSWORD=your_secure_password

# ============================================
# SESSION & SECURITY
# ============================================
SESSION_DRIVER=database
# Session lifetime in minutes (120 = 2 hours)
SESSION_LIFETIME=120
# Encrypt session data (recommended for sensitive data)
SESSION_ENCRYPT=false
SESSION_PATH=/
# Set to your domain in production (e.g., .bumiasiparahyangan.com)
SESSION_DOMAIN=null

# PRODUCTION SECURITY: Set secure session cookies
# SESSION_SECURE_COOKIE=true
# SESSION_HTTP_ONLY=true
# SESSION_SAME_SITE=lax

# ============================================
# CACHE CONFIGURATION
# ============================================
# Cache driver: file, database, redis, memcached
CACHE_STORE=database
# Optional cache prefix to avoid key collisions
# CACHE_PREFIX=bap_

# ============================================
# QUEUE & BACKGROUND JOBS
# ============================================
# Queue driver: sync (immediate), database, redis, sqs
QUEUE_CONNECTION=database

# ============================================
# BROADCASTING
# ============================================
BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local

# ============================================
# REDIS (Optional - for caching/queues)
# ============================================
REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

# ============================================
# MAIL CONFIGURATION
# ============================================
# Driver: smtp, sendmail, mailgun, ses, postmark, log
MAIL_MAILER=log
MAIL_SCHEME=null
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
# IMPORTANT: Update for production
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

# Production SMTP Example (Gmail, SendGrid, Mailgun, etc.):
# MAIL_MAILER=smtp
# MAIL_HOST=smtp.gmail.com
# MAIL_PORT=587
# MAIL_USERNAME=your-email@gmail.com
# MAIL_PASSWORD=your-app-password
# MAIL_ENCRYPTION=tls
# MAIL_FROM_ADDRESS="noreply@bumiasiparahyangan.com"

# ============================================
# AWS SERVICES (Optional - S3, SES, etc.)
# ============================================
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

# ============================================
# THIRD-PARTY SERVICES (Optional)
# ============================================
# Google Analytics, reCAPTCHA, etc.
# GOOGLE_ANALYTICS_ID=
# RECAPTCHA_SITE_KEY=
# RECAPTCHA_SECRET_KEY=

# ============================================
# FRONTEND BUILD
# ============================================
VITE_APP_NAME="${APP_NAME}"

# ============================================
# PRODUCTION CHECKLIST
# ============================================
# Before deploying to production:
# 1. ✓ Set APP_ENV=production
# 2. ✓ Set APP_DEBUG=false
# 3. ✓ Generate APP_KEY with 'php artisan key:generate'
# 4. ✓ Update APP_URL to production domain
# 5. ✓ Configure production database credentials
# 6. ✓ Set up real mail driver (SMTP, Mailgun, SES)
# 7. ✓ Update MAIL_FROM_ADDRESS with your domain
# 8. ✓ Set SESSION_DOMAIN to your domain
# 9. ✓ Enable SESSION_SECURE_COOKIE=true for HTTPS
# 10. ✓ Run 'php artisan config:cache'
# 11. ✓ Run 'php artisan route:cache'
# 12. ✓ Run 'php artisan view:cache'
# 13. ✓ Run 'npm run build' for production assets
# 14. ✓ Set proper file permissions (storage/, bootstrap/cache/)
# 15. ✓ Configure web server (Nginx/Apache) with proper security headers

